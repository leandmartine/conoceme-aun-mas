# Backend
PORT=8787
HOST=0.0.0.0
CONTENT_ROOT=./content
CORS_ORIGIN=http://localhost:5173

# Future: API keys for AI / integrations (comma-separated). Never commit real keys.
# PORTFOLIO_API_KEYS=dev-key-change-me
# AI_PROVIDER_KEY=
