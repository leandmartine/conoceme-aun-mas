# Backend
PORT=8787
HOST=0.0.0.0
CONTENT_ROOT=./content
CORS_ORIGIN=http://localhost:5173

# API keys for AI companion (comma-separated). Never commit real keys.
# Required for POST /api/v1/ai/chat (Authorization: Bearer <key> or X-Api-Key).
PORTFOLIO_API_KEYS=dev-local-key

# Optional: SpaceXAI / xAI for future LLM mode (server-only; never expose to browser).
# XAI_API_KEY=
# AI_RATE_LIMIT_MAX=30
# AI_RATE_LIMIT_WINDOW_MS=60000

# Frontend (Vite) — same dev key as above for local companion UI
# VITE_PORTFOLIO_API_KEY=dev-local-key
