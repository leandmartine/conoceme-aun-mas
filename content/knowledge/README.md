# Knowledge base (AI companion)

Markdown facts for the grounded companion (`POST /api/v1/ai/chat`).

- Only public portfolio truth — never invent employment or titles.  
- Updated when `profile.json` or place copy changes.  
- Loaded by the backend from this folder (server-side only).  
